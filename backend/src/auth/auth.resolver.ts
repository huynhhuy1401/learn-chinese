// Supabase handles auth on the frontend.
// Backend auth is guard-only (JWT verification).
// This file is intentionally lean — no register/login mutations needed.
// The 'me' query lives in UsersResolver.
export {};
