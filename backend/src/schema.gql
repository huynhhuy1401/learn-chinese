# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Course {
  description: String!
  id: ID!
  imageUrl: String
  lessonCount: Int
  level: String!
  name: String!
  provinces: [Province!]
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Exercise {
  correctAnswer: String!
  id: ID!
  options: String!
  question: String!
  questionLabel: String
  type: String!
}

type ExerciseResult {
  correct: Boolean!
  correctAnswer: String!
}

type GrammarPoint {
  examples: String!
  explanation: String!
  id: ID!
  title: String!
}

type Mutation {
  completeProvince(provinceId: ID!): UserProgress!
  reviewWord(correct: Boolean!, wordId: ID!): UserWordProgress!
  saveWord(wordId: ID!): SavedWordResult!
  submitAnswer(answer: String!, exerciseId: String!, provinceId: ID!): ExerciseResult!
  unsaveWord(wordId: ID!): Boolean!
}

type Province {
  capital: String!
  color: String!
  culturalDescription: String!
  custom: String!
  exercises: [Exercise!]
  food: String!
  foodDescription: String!
  grammar: [GrammarPoint!]
  id: ID!
  imageUrl: String
  landmark: String!
  landmarkFact: String!
  mapCoordinates: String!
  name: String!
  storyContent: String
  unlockOrder: Int!
  vocabulary: [VocabularyWord!]
}

type Query {
  course(id: ID!): Course
  courses: [Course!]!
  exercises(provinceId: ID!): [Exercise!]!
  grammar(provinceId: ID): [GrammarPoint!]!
  isWordSaved(wordId: ID!): Boolean!
  me: User!
  province(id: ID!): Province
  provinces: [Province!]!
  savedWords: [SavedWordResult!]!
  userProgress: [UserProgress!]!
  vocabulary(provinceId: ID): [VocabularyWord!]!
  word(id: ID!): VocabularyWord
  wordProgress: [UserWordProgress!]!
}

type SavedWordResult {
  createdAt: DateTime!
  id: ID!
  word: VocabularyWord!
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
}

type UserProgress {
  completed: Boolean!
  exercisesDone: Int!
  id: ID!
  lastStudiedAt: DateTime!
  provinceId: String!
  score: Int!
  userId: String!
}

type UserWordProgress {
  id: ID!
  lastReviewedAt: DateTime!
  masteryLevel: Int!
  nextReviewAt: DateTime!
  timesCorrect: Int!
  timesWrong: Int!
  userId: String!
  wordId: String!
}

type VocabularyWord {
  category: String!
  character: String!
  english: String!
  id: ID!
  masteryLevel: Float
  pinyin: String!
  province: WordProvince
  travelSentence: String
}

type WordProvince {
  color: String!
  name: String!
}